language: java
jdk: openjdk8

services:
  - docker

after_success:
  - docker login -u $DOCKER_USER -p $DOCKER_PASS
  - docker build -t fyp-backend/dive-rest-service ./diveService
  - docker push fyp-backend/dive-rest-service